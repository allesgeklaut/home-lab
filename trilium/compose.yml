services:
  trilium:
    #    image: zadam/trilium:latest
    image: triliumnext/trilium:latest
    container_name: trilium
    restart: unless-stopped
    environment:
      TRILIUM_DATA_DIR: /home/node/trilium-data
    volumes:
      - /opt/stacks/trilium/data:/home/node/trilium-data
    ports:
      - "9910:8080"
