# code-server configuration secrets (real file lives in /opt/secrets/, git-ignored)
# HASHED_PASSWORD must be the SHA-256 hash of your password, hex-encoded:
#   printf "yourpassword" | sha256sum | cut -d' ' -f1
# Leave empty to use the auto-generated password in the container logs instead.
HASHED_PASSWORD=